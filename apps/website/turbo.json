{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!.next/**"],
      "passThroughEnv": ["DATABASE_URL", "PAYLOAD_SECRET"]
    }
  }
}
